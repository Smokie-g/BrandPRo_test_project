import { connect } from "react-redux";
import CharacterDetailScreenComponent from "./CharacterDetailScreen";

const mapStateToProps = state => ({});

const mapDispatchToProps = {};

export const CharacterDetailScreen = connect(
  mapStateToProps,
  mapDispatchToProps
)(CharacterDetailScreenComponent);